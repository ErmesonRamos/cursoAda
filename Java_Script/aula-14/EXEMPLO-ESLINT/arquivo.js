/*
  ! JSON - JavaScript Object Notation ( Notações de objetos JavaScript );

  ? Aplicação JSON
    * 1. Arquivos de configurações:
        a. Configurações do VS code
        b. package.json
        c. .prettier.json
        d. .eslint.json

    * 2. A transferência de dados entre aplicações: API.

    * 3. Lidando com JSON com JavaScript
        1. Lendo um arquivo JSON
        2. Convertendo JSON, em formato de string, para um objeto
        3. Convertendo um objeto do JS, em um JSON (string)
*/

const aula = "JSON";

console.log(aula);
